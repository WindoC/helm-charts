{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.ingress.name }}
  namespace: {{ include "guacamole.namespace" . | quote }}
  labels:
    {{- include "guacamole.commonLabels" . | nindent 4 }}
  annotations:
    nginx.ingress.kubernetes.io/proxy-cookie-path: /guacamole/ /
    nginx.ingress.kubernetes.io/rewrite-target: /guacamole/$1
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if .Values.ingress.ingressClassName }}
  ingressClassName: {{ .Values.ingress.ingressClassName }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - backend:
              service:
                name: {{ .Values.names.guacamoleService }}
                port:
                  name: guacamole
            path: {{ .Values.ingress.path }}
            pathType: {{ .Values.ingress.pathType }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.ingress.host }}
      secretName: {{ .Values.ingress.tls.secretName }}
  {{- end }}
{{- end }}
